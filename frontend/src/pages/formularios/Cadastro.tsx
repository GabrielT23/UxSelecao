import { LayoutFormulario } from "../../shared/layout";
import { CadastroForm } from "../../shared/components";

export const Cadastro = ()=>{

    return(
        <LayoutFormulario>
            <CadastroForm/>
        </LayoutFormulario>
    );
}